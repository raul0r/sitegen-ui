# Server-side only. Never prefix these with VITE_.
# The Vite/Cloudflare proxy injects SITEGEN_API_TOKEN. Browser JS must not read it.

SITEGEN_API_BASE_URL=http://127.0.0.1:8000
SITEGEN_API_TOKEN=

# Docker Compose UI container should use:
# SITEGEN_API_BASE_URL=http://host.docker.internal:8000
